export interface BuyItemType {
  id: number;
  category: string;
  product: string;
  multiply: number;
  price: number;
  email: string;
}

export interface BuyProductType {
  id: number;
  category: string;
  product: string;
}
