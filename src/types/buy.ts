export interface BuyItemType {
  id: number;
  category: string;
  product: string;
  multiply: string;
  price: string;
  email: string;
}

export interface BuyProductType {
  id: number;
  category: string;
  product: string;
}
